module.exports = {
    host: 'your-email-host',
    port: 587, // or the appropriate port for your email service
    auth: {
      user: 'your-email-address',
      pass: 'your-email-password'
    },
    from: 'your-email-address' // The email address to use as the sender
  };